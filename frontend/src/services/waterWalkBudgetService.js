export const GRANT_JOB_OPTIONS = [
  {
    id: 'uk-england.capital-grants-2026/rp32-small-leaky-woody-dams',
    label: 'RP32 small leaky woody dam',
    interventionType: 'check-dam',
    unit: 'dam',
    paymentType: 'per_item',
    grantAmountPerUnit: 461.39,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp33-large-leaky-woody-dams',
    label: 'RP33 large leaky woody dam',
    interventionType: 'check-dam',
    unit: 'dam',
    paymentType: 'per_item',
    grantAmountPerUnit: 764.42,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn12-create-or-restore-ponds-up-to-2ha',
    label: 'WN12 create or restore pond',
    interventionType: 'pond',
    unit: 'pond',
    paymentType: 'per_item',
    grantAmountPerUnit: 1879.46,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp3-watercourse-crossings',
    label: 'RP3 watercourse crossing',
    interventionType: 'water-infrastructure',
    unit: 'crossing',
    paymentType: 'per_item',
    grantAmountPerUnit: 506.44,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp5-cross-drains',
    label: 'RP5 cross drain',
    interventionType: 'runoff-control',
    unit: 'drain',
    paymentType: 'per_item',
    grantAmountPerUnit: 750.26,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp6-piped-culverts-in-ditches',
    label: 'RP6 piped culvert in ditch',
    interventionType: 'water-infrastructure',
    unit: 'culvert',
    paymentType: 'per_item',
    grantAmountPerUnit: 376.23,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp7-sediment-ponds-and-traps',
    label: 'RP7 sediment pond or trap',
    interventionType: 'runoff-control',
    unit: 'square metre',
    paymentType: 'per_area',
    grantAmountPerUnit: 11.88,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp8-constructed-wetlands-for-pollution',
    label: 'RP8 constructed wetland for pollution',
    interventionType: 'wetland-restoration',
    unit: 'eligible cash cost',
    paymentType: 'percent_actual_cost',
    grantAmountPerUnit: 50,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp9-earth-banks-and-soil-bunds',
    label: 'RP9 earth bank or soil bund',
    interventionType: 'runoff-control',
    unit: '100 metres',
    paymentType: 'per_length_unit',
    grantAmountPerUnit: 195.61,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp10-silt-filtration-dams-or-seepage-barriers',
    label: 'RP10 silt filtration dam or seepage barrier',
    interventionType: 'runoff-control',
    unit: 'unit',
    paymentType: 'per_item',
    grantAmountPerUnit: 198.14,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp11-swales',
    label: 'RP11 swale',
    interventionType: 'runoff-control',
    unit: 'square metre',
    paymentType: 'per_area',
    grantAmountPerUnit: 7.52,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/rp12-check-dams',
    label: 'RP12 check dam',
    interventionType: 'check-dam',
    unit: 'dam',
    paymentType: 'per_item',
    grantAmountPerUnit: 85.29,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn1-grip-blocking-drainage-channels',
    label: 'WN1 grip blocking drainage channel',
    interventionType: 'wetland-restoration',
    unit: 'block',
    paymentType: 'per_item',
    grantAmountPerUnit: 19.06,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn2-creation-of-scrapes-and-gutters',
    label: 'WN2 create scrape or gutter',
    interventionType: 'wetland-restoration',
    unit: 'square metre',
    paymentType: 'per_area',
    grantAmountPerUnit: 5.64,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn3-ditch-dyke-and-rhine-restoration',
    label: 'WN3 restore ditch, dyke or rhine',
    interventionType: 'wetland-restoration',
    unit: 'metre',
    paymentType: 'per_length',
    grantAmountPerUnit: 5.72,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn4-ditch-dyke-and-rhine-creation',
    label: 'WN4 create ditch, dyke or rhine',
    interventionType: 'wetland-restoration',
    unit: 'metre',
    paymentType: 'per_length',
    grantAmountPerUnit: 13.63,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn7-restoration-of-large-water-bodies',
    label: 'WN7 restore large water body',
    interventionType: 'pond',
    unit: 'eligible cash cost',
    paymentType: 'percent_actual_cost',
    grantAmountPerUnit: 100,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn8-timber-sluice',
    label: 'WN8 timber sluice',
    interventionType: 'water-control-structure',
    unit: 'sluice',
    paymentType: 'per_item',
    grantAmountPerUnit: 451.5,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn9-brick-stone-or-concrete-sluice',
    label: 'WN9 brick, stone or concrete sluice',
    interventionType: 'water-control-structure',
    unit: 'sluice',
    paymentType: 'per_item',
    grantAmountPerUnit: 2901.4,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn10-construction-of-water-penning-structures',
    label: 'WN10 water penning structure',
    interventionType: 'water-control-structure',
    unit: 'eligible cash cost',
    paymentType: 'percent_actual_cost',
    grantAmountPerUnit: 100,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn11-temporary-water-storage-features',
    label: 'WN11 temporary water storage feature',
    interventionType: 'runoff-control',
    unit: 'square metre',
    paymentType: 'per_area',
    grantAmountPerUnit: 7.36,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/wn13-manage-wetland-cutting-supplement',
    label: 'WN13 wetland cutting supplement',
    interventionType: 'wetland-restoration',
    unit: 'hectare',
    paymentType: 'per_area',
    grantAmountPerUnit: 1346,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/te1-planting-standard-hedgerow-tree',
    label: 'TE1 standard hedgerow tree',
    interventionType: 'tree-planting',
    unit: 'tree',
    paymentType: 'per_item',
    grantAmountPerUnit: 19.06,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/te2-planting-standard-parkland-tree',
    label: 'TE2 standard parkland tree',
    interventionType: 'tree-planting',
    unit: 'tree',
    paymentType: 'per_item',
    grantAmountPerUnit: 123.94,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/te3-planting-fruit-trees',
    label: 'TE3 fruit tree',
    interventionType: 'tree-planting',
    unit: 'tree',
    paymentType: 'per_item',
    grantAmountPerUnit: 28.07,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/te4-supply-and-plant-tree',
    label: 'TE4 supply and plant tree',
    interventionType: 'tree-planting',
    unit: 'tree',
    paymentType: 'per_item',
    grantAmountPerUnit: 1.72,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/te15-plant-groups-of-trees-in-large-guards',
    label: 'TE15 tree group in large guard',
    interventionType: 'tree-planting',
    unit: 'large guard',
    paymentType: 'per_item',
    grantAmountPerUnit: 572.78,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/af1-plant-agroforestry-woodland-tree',
    label: 'AF1 agroforestry woodland tree',
    interventionType: 'tree-planting',
    unit: 'tree',
    paymentType: 'per_item',
    grantAmountPerUnit: 5.4,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/af2-plant-agroforestry-fruit-tree',
    label: 'AF2 agroforestry fruit tree',
    interventionType: 'tree-planting',
    unit: 'tree',
    paymentType: 'per_item',
    grantAmountPerUnit: 17.83,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/af3-species-diversity-bonus-supplement',
    label: 'AF3 species diversity bonus supplement',
    interventionType: 'tree-planting',
    unit: 'tree',
    paymentType: 'per_item',
    grantAmountPerUnit: 1.16,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/gr1-create-or-restore-grassland-habitat',
    label: 'GR1 create or restore grassland habitat',
    interventionType: 'grassland-habitat',
    unit: 'hectare',
    paymentType: 'per_area',
    grantAmountPerUnit: 186.02,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/gr2-standard-seed-mix-for-grassland-habitat',
    label: 'GR2 standard grassland seed mix',
    interventionType: 'grassland-habitat',
    unit: 'hectare',
    paymentType: 'per_area',
    grantAmountPerUnit: 203,
    currency: 'GBP',
  },
  {
    id: 'uk-england.capital-grants-2026/gr3-floristically-enhanced-seed-mix-for-grassland-habitat',
    label: 'GR3 floristically enhanced grassland seed mix',
    interventionType: 'grassland-habitat',
    unit: 'hectare',
    paymentType: 'per_area',
    grantAmountPerUnit: 890.02,
    currency: 'GBP',
  },
];

export const BUDGET_CONFIDENCE = ['low', 'medium', 'high'];
export const LANDOWNER_JUDGEMENTS = ['worth_exploring', 'needs_quote_or_adviser', 'not_worth_it'];
export const BUDGET_LIFECYCLE_STAGES = ['first_estimate', 'planned_estimate', 'in_progress', 'actuals_entered', 'reviewed'];

export function grantJobOptionById(optionId) {
  return GRANT_JOB_OPTIONS.find(option => option.id === optionId) || GRANT_JOB_OPTIONS[0];
}

export function numberOrZero(value) {
  const number = Number(value);
  return Number.isFinite(number) ? number : 0;
}

export function positiveNumberOrDefault(value, fallback = 1) {
  const number = Number(value);
  return Number.isFinite(number) && number > 0 ? number : fallback;
}

export function splitLines(value = '') {
  return String(value)
    .split(/\n+/)
    .map(line => line.trim())
    .filter(Boolean);
}

export function calculateGrantJobBudget({
  optionId,
  quantity = 1,
  cashCost = 0,
  internalCost = 0,
  actualGrantIncome = '',
  actualCashCost = '',
  actualInternalCost = '',
}) {
  const option = grantJobOptionById(optionId);
  const safeQuantity = positiveNumberOrDefault(quantity);
  const safeCashCost = numberOrZero(cashCost);
  const safeInternalCost = numberOrZero(internalCost);
  let grantIncome = null;
  if (Number.isFinite(option.grantAmountPerUnit)) {
    grantIncome = option.paymentType === 'percent_actual_cost'
      ? Number((safeCashCost * (option.grantAmountPerUnit / 100)).toFixed(2))
      : Number((option.grantAmountPerUnit * safeQuantity).toFixed(2));
  }
  const margin = grantIncome === null
    ? null
    : Number((grantIncome - safeCashCost - safeInternalCost).toFixed(2));
  const hasActualGrantIncome = actualGrantIncome !== '' && actualGrantIncome !== null && actualGrantIncome !== undefined;
  const hasActualCashCost = actualCashCost !== '' && actualCashCost !== null && actualCashCost !== undefined;
  const hasActualInternalCost = actualInternalCost !== '' && actualInternalCost !== null && actualInternalCost !== undefined;
  const actualGrant = hasActualGrantIncome ? numberOrZero(actualGrantIncome) : null;
  const actualCash = hasActualCashCost ? numberOrZero(actualCashCost) : null;
  const actualInternal = hasActualInternalCost ? numberOrZero(actualInternalCost) : null;
  const actualMargin = actualGrant === null
    ? null
    : Number((actualGrant - (actualCash || 0) - (actualInternal || 0)).toFixed(2));
  const delta = (actual, estimate) => (
    actual === null || estimate === null ? null : Number((actual - estimate).toFixed(2))
  );

  return {
    option,
    quantity: safeQuantity,
    grantIncome,
    cashCost: safeCashCost,
    internalCost: safeInternalCost,
    margin,
    actualGrantIncome: actualGrant,
    actualCashCost: actualCash,
    actualInternalCost: actualInternal,
    actualMargin,
    variance: {
      grantIncomeDelta: delta(actualGrant, grantIncome),
      cashCostDelta: delta(actualCash, safeCashCost),
      internalCostDelta: delta(actualInternal, safeInternalCost),
      marginDelta: delta(actualMargin, margin),
    },
  };
}

export function buildGrantJobBudgetRecord({
  existing = null,
  site,
  target,
  form,
  now = new Date().toISOString(),
}) {
  const calculation = calculateGrantJobBudget(form);
  const confidence = BUDGET_CONFIDENCE.includes(form.confidence) ? form.confidence : 'low';
  const landownerJudgement = LANDOWNER_JUDGEMENTS.includes(form.landownerJudgement)
    ? form.landownerJudgement
    : 'needs_quote_or_adviser';
  const lifecycleStage = BUDGET_LIFECYCLE_STAGES.includes(form.lifecycleStage)
    ? form.lifecycleStage
    : 'first_estimate';

  return {
    schemaVersion: 'jobdone.waterWalkGrantJobBudget.v2',
    id: existing?.id || `water-walk-budget-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
    createdAt: existing?.createdAt || now,
    updatedAt: now,
    siteId: site.id,
    siteLabel: site.label,
    targetType: target.type,
    targetId: target.id,
    targetTitle: target.title,
    interventionType: calculation.option.interventionType,
    fundingOptionId: calculation.option.id,
    fundingOptionName: calculation.option.label,
    quantity: {
      amount: calculation.quantity,
      unit: calculation.option.unit,
    },
    grantIncomeEstimate: {
      amount: calculation.grantIncome,
      currency: calculation.option.currency,
      confidence,
    },
    cashCostEstimate: {
      amount: calculation.cashCost,
      currency: calculation.option.currency,
      confidence,
    },
    internalCostEstimate: {
      amount: calculation.internalCost,
      currency: calculation.option.currency,
      confidence,
    },
    marginEstimate: {
      amount: calculation.margin,
      currency: calculation.option.currency,
      confidence,
    },
    actualGrantIncome: {
      amount: calculation.actualGrantIncome,
      currency: calculation.option.currency,
    },
    actualCashCost: {
      amount: calculation.actualCashCost,
      currency: calculation.option.currency,
    },
    actualInternalCost: {
      amount: calculation.actualInternalCost,
      currency: calculation.option.currency,
    },
    actualMargin: {
      amount: calculation.actualMargin,
      currency: calculation.option.currency,
    },
    variance: calculation.variance,
    resourceNotes: {
      machinery: String(form.machineryNotes || '').trim(),
      labour: String(form.labourNotes || '').trim(),
      materials: String(form.materialsNotes || '').trim(),
    },
    unknowns: splitLines(form.unknownsText),
    confidence,
    landownerJudgement,
    lifecycleStage,
    outcomeReview: {
      wentBetterThanPlanned: splitLines(form.wentBetterText),
      wentWorseThanPlanned: splitLines(form.wentWorseText),
      varianceExplanation: String(form.varianceExplanation || '').trim(),
      lessonForNextTime: String(form.lessonForNextTime || '').trim(),
    },
  };
}

export function budgetForTarget(budgets = [], target) {
  if (!target) return null;
  return budgets.find(budget => budget.targetType === target.type && budget.targetId === target.id) || null;
}

export function upsertBudget(budgets = [], budget) {
  const withoutExisting = budgets.filter(item => item.id !== budget.id);
  return [budget, ...withoutExisting];
}

export function budgetToForm(budget = null) {
  if (!budget) {
    return {
      optionId: GRANT_JOB_OPTIONS[0].id,
      quantity: 1,
      cashCost: 0,
      internalCost: 0,
      actualGrantIncome: '',
      actualCashCost: '',
      actualInternalCost: '',
      confidence: 'low',
      landownerJudgement: 'needs_quote_or_adviser',
      lifecycleStage: 'first_estimate',
      machineryNotes: '',
      labourNotes: '',
      materialsNotes: '',
      unknownsText: '',
      wentBetterText: '',
      wentWorseText: '',
      varianceExplanation: '',
      lessonForNextTime: '',
    };
  }

  return {
    optionId: budget.fundingOptionId || GRANT_JOB_OPTIONS[0].id,
    quantity: budget.quantity?.amount || 1,
    cashCost: budget.cashCostEstimate?.amount || 0,
    internalCost: budget.internalCostEstimate?.amount || 0,
    actualGrantIncome: budget.actualGrantIncome?.amount ?? '',
    actualCashCost: budget.actualCashCost?.amount ?? '',
    actualInternalCost: budget.actualInternalCost?.amount ?? '',
    confidence: budget.confidence || 'low',
    landownerJudgement: budget.landownerJudgement || 'needs_quote_or_adviser',
    lifecycleStage: budget.lifecycleStage || 'first_estimate',
    machineryNotes: budget.resourceNotes?.machinery || '',
    labourNotes: budget.resourceNotes?.labour || '',
    materialsNotes: budget.resourceNotes?.materials || '',
    unknownsText: Array.isArray(budget.unknowns) ? budget.unknowns.join('\n') : '',
    wentBetterText: Array.isArray(budget.outcomeReview?.wentBetterThanPlanned) ? budget.outcomeReview.wentBetterThanPlanned.join('\n') : '',
    wentWorseText: Array.isArray(budget.outcomeReview?.wentWorseThanPlanned) ? budget.outcomeReview.wentWorseThanPlanned.join('\n') : '',
    varianceExplanation: budget.outcomeReview?.varianceExplanation || '',
    lessonForNextTime: budget.outcomeReview?.lessonForNextTime || '',
  };
}

export function formatBudgetMoney(amount, currency = 'GBP') {
  if (amount === null || amount === undefined || !Number.isFinite(Number(amount))) return 'Unknown';
  return `${currency} ${Number(amount).toFixed(2)}`;
}

export function formatGrantRate(option) {
  if (!option || !Number.isFinite(option.grantAmountPerUnit)) return 'Grant payment is unknown in the current seed data.';
  if (option.paymentType === 'percent_actual_cost') {
    return `Grant estimate uses ${Number(option.grantAmountPerUnit).toFixed(0)}% of entered cash cost.`;
  }
  return `Grant estimate uses ${formatBudgetMoney(option.grantAmountPerUnit, option.currency)} per ${option.unit}.`;
}
